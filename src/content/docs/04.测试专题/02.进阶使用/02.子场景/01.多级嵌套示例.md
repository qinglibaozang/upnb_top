---
title: 多级嵌套示例
description: 用于测试三层目录嵌套的侧边栏展示效果。
# lastUpdated: 2026-08-16 12:00:00
# 正文宽度：wide(1200px) / full(占满) / 或 CSS 长度如 900px；删掉该行则用全局默认
---

## 本页位置

本页位于 `04.测试专题 / 02.进阶使用 / 02.子场景` 目录下，用于验证：

1. 三层嵌套侧边栏的缩进与引导线
2. 文件夹图标的展开/收起切换
3. 数字序号前缀是否被正确剥离

## 预期效果

- 侧边栏显示：进阶使用 ▸ 子场景 ▸ 多级嵌套示例
- 图标与上一级文字左对齐
