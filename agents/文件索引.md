<!-- ============================================================
 文件用途：关键文件索引（改什么 → 去哪）
 给 AI 看：本文件由 AGENTS.md 通过 @agents/文件索引.md 引用，自动内联到
           系统提示词。接到任务先查此表定位文件。
 给人类看：新增文件时在此登记入口，方便查找。
============================================================ -->

| 任务 | 去哪 |
|---|---|
| 改内容 | `src/content/docs/`（对应分类文件夹） |
| 改导航/侧边栏 | `astro.config.mjs`（自动分类逻辑） |
| 改站点身份（站名/logo/导航/语言） | `astro.config.mjs`（`title`/`logo`/`locales`/`nav` 字段） |
| 改自定义组件 | `src/components/`（8 个覆盖组件：`Header`、`TwoColumnContent`、`Head`、`Search`、`Footer`、`MobileMenuFooter`、`PageTitle`、`SkipLink`，均在 `astro.config.mjs` 注册） |
| 改排版/样式 | `src/styles/zh.css` |
| 改中文优化规则 | `src/scripts/zh-optimize.ts` |
| 改站点配置 | `astro.config.mjs` |
| **新增/改页面数据结构** | **`agents/数据结构.md`（先登记，再实现）** |
| 加静态资源 | `public/` 或 `src/assets/` |
