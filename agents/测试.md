<!-- ============================================================
 文件用途：测试约定
 给 AI 看：本文件由 AGENTS.md 通过 @agents/测试.md 引用，自动内联到系统
           提示词。改动行为后按此验证。
 给人类看：改测试约定编辑本文件。
============================================================ -->

- 项目无测试框架（无 vitest/jest）。纯函数（`src/scripts/zh-optimize.ts`）可在改动后用 `node` 直接验证或临时断言。
- 改动行为后：类型检查 `npx tsc --noEmit` + 实际构建/预览验证，不依赖自动化测试套件。
